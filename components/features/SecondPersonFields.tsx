import type { SecondPersonInput } from "@/lib/horoscope/types";
export const emptySecondPerson: SecondPersonInput = { fullName: "", dateOfBirth: "", birthTime: "", birthTimeKnown: true, birthPlace: "" };
export default function SecondPersonFields({ value, onChange }: { value: SecondPersonInput; onChange: (value: SecondPersonInput) => void }) {
  const field = "mt-2 min-h-12 w-full rounded-xl border border-white/10 bg-[#020817]/70 px-3 text-white outline-none focus:border-sky-400/50";
  return <div className="grid gap-4 sm:grid-cols-2"><label className="text-sm text-white/60">Full name<input required maxLength={100} className={field} value={value.fullName} onChange={(e) => onChange({ ...value, fullName: e.target.value })} /></label><label className="text-sm text-white/60">Date of birth<input required type="date" max={new Date().toISOString().slice(0,10)} className={field} value={value.dateOfBirth} onChange={(e) => onChange({ ...value, dateOfBirth: e.target.value })} /></label><label className="text-sm text-white/60">Birth time<input required={value.birthTimeKnown} disabled={!value.birthTimeKnown} type="time" className={field} value={value.birthTime} onChange={(e) => onChange({ ...value, birthTime: e.target.value })} /></label><label className="text-sm text-white/60">Birth place<input required maxLength={160} className={field} value={value.birthPlace} onChange={(e) => onChange({ ...value, birthPlace: e.target.value })} /></label><label className="flex items-center gap-2 text-sm text-white/60 sm:col-span-2"><input type="checkbox" checked={!value.birthTimeKnown} onChange={(e) => onChange({ ...value, birthTimeKnown: !e.target.checked, birthTime: e.target.checked ? "" : value.birthTime })} /> Birth time is unknown</label></div>;
}
